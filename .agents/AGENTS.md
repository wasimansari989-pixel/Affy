# Project Rules

## Git Commit & Push Rules
- **Ask Before Commit & Push**: Never execute `git commit` or `git push` automatically. Always ask the user for explicit approval before staging, committing, or pushing code to any Git remote.
